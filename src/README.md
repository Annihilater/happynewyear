# 📁 前端源代码

这个目录包含了新年倒计时网站的所有前端代码。

## 📂 目录结构

```
src/
├── index.html          # 主页面入口
├── css/
│   └── style.css       # 样式文件（CSS3 + 动画）
└── js/
    ├── main.js         # 主控制器（ES6 Module）
    ├── fireworks.js    # Three.js 3D烟花系统
    ├── starfield.js    # Canvas 2D星空背景
    ├── audio.js        # Web Audio深度音效
    ├── config.js       # 配置管理+双语+LocalStorage
    ├── countdown.js    # 倒计时逻辑
    └── danmaku.js      # 弹幕祝福系统
```

## 🚀 本地运行

```bash
# 方式1: 直接打开
open index.html

# 方式2: Python服务器（默认端口5861）
python3 -m http.server 5861
# 访问 http://localhost:5861

# 方式3: 使用项目根目录的Makefile
cd ..
make dev
```

## 🎨 主要文件说明

### index.html
- Three.js importmap配置
- HTML结构（星空、烟花Canvas、倒计时、弹幕、设置面板）
- ES6 Module导入

### css/style.css
- CSS变量定义
- 渐变和发光效果
- 响应式布局
- 弹幕动画
- 设置面板样式

### js/fireworks.js
- **Three.js WebGL 3D渲染**
- UnrealBloomPass后期处理
- 23000粒子爆炸系统
- Mono/Dual/Tri颜色系统
- 悬停+下落物理模拟

### js/starfield.js
- Canvas 2D星空
- 缓慢漂移动画
- 闪烁效果

### js/audio.js
- Web Audio API深度音效
- 三层声音合成（低频+噪音+冲击）
- DynamicsCompressor

### js/config.js
- 参数配置管理
- 中英文双语
- LocalStorage持久化
- 模式切换（舒缓/激烈）
- 定时切换

### js/countdown.js
- 倒计时计算
- 翻牌动画
- 到达时间回调

### js/danmaku.js
- 80+预设祝福语
- 用户愿望高亮
- 轨道占用管理

### js/main.js
- 模块初始化
- 事件绑定（鼠标、键盘）
- 设置面板控制
- 新年庆祝逻辑

## 🔧 修改指南

### 修改烟花参数

编辑 `js/config.js`：

```javascript
particles: {
    particleCount: 23000,  // 粒子数量
    particleSize: 0.8,     // 粒子大小
    fadeSpeed: 0.00482     // 淡出速度
}
```

### 添加新祝福语

编辑 `js/danmaku.js`，在 `wishes` 数组中添加：

```javascript
this.wishes = [
    "你的新祝福语",
    // ...现有祝福语
];
```

### 修改主题色

编辑 `css/style.css`：

```css
:root {
    --accent-purple: #a855f7;  /* 紫色主题 */
    --accent-cyan: #22d3ee;    /* 青色辅助 */
}
```

## 📝 开发注意事项

1. **ES6 Module**：`fireworks.js` 和 `main.js` 使用 `type="module"`
2. **Three.js依赖**：使用CDN importmap，无需本地安装
3. **跨域问题**：本地开发必须使用HTTP服务器，不能直接打开HTML
4. **浏览器兼容**：需要支持ES6 Module的现代浏览器

## 🐳 Docker构建

Docker会自动复制此目录到nginx容器：

```dockerfile
COPY src/ /usr/share/nginx/html/
```

---

**修改代码后记得重新构建Docker镜像！**

```bash
cd ../deploy
./build.sh
```
