# 🎆 2026新年倒计时网站 - 设计与规划文档

> 参考网站: https://2026.xcodeman.com/

## 📋 项目概述

这是一个精美的新年倒计时网页，包含烟花特效、星空背景、弹幕祝福语等功能，用于迎接2026年的到来。

---

## 🎯 功能清单

### 核心功能

| 序号 | 功能 | 描述 | 优先级 |
|------|------|------|--------|
| 1 | 星空粒子背景 | 3D飞行效果，粒子从中心向四周发散 | P0 |
| 2 | 烟花特效 | 随机位置发射，带拖尾和爆炸效果 | P0 |
| 3 | 倒计时器 | 翻牌样式，显示天/时/分/秒 | P0 |
| 4 | 弹幕祝福语 | 彩色文字从右向左飘动 | P0 |
| 5 | 愿望输入框 | 用户输入愿望后发射到弹幕 | P1 |
| 6 | 音效系统 | 点击启用后播放烟花音效 | P1 |
| 7 | 音频提示覆盖层 | 点击激活音频并发射烟花 | P1 |

### 视觉设计

- **背景色**: `#050505` (深黑色)
- **主色调**: 紫色渐变 (`purple-400` ~ `purple-600`)
- **辅助色**: 青色 (`cyan-400`)
- **文字颜色**: 白色/灰色

---

## 🏗️ 技术架构

### 技术选型

| 技术 | 用途 |
|------|------|
| HTML5 | 页面结构 |
| CSS3 | 样式、动画、响应式 |
| Canvas 2D | 烟花和星空粒子渲染 |
| JavaScript (ES6+) | 交互逻辑 |
| Web Audio API | 音效播放 |

### 文件结构

```
happynewyear/
├── index.html              # 主页面
├── css/
│   └── style.css           # 样式文件
├── js/
│   ├── main.js             # 主逻辑
│   ├── starfield.js        # 星空背景
│   ├── fireworks.js        # 烟花系统
│   ├── countdown.js        # 倒计时
│   └── danmaku.js          # 弹幕系统
├── audio/
│   └── firework.mp3        # 烟花音效 (需自行添加)
├── DESIGN.md               # 本文档
└── README.md               # 使用说明
```

---

## 🎨 UI设计规范

### 布局结构

```
┌─────────────────────────────────────────────────────────┐
│  [点击此处唤醒音频并发射烟花] (覆盖层)                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ← 弹幕祝福语飘过 ←                                      │
│                                                         │
│                   APPROACHING                           │
│                      2026                               │
│               (大号渐变发光文字)                          │
│                                                         │
│          ┌─────────────────────────┐                    │
│          │  00  07  54  26         │                    │
│          │ DAYS HRS MIN SEC        │                    │
│          └─────────────────────────┘                    │
│                                                         │
│          [输入你的2026新年愿望...  🚀]                   │
│                                                         │
│  ← 弹幕祝福语飘过 ←                                      │
│                                                         │
└─────────────────────────────────────────────────────────┘
          (星空粒子背景 + 随机烟花)
```

### 颜色定义

```css
:root {
  --bg-primary: #050505;
  --bg-secondary: #0a0a0a;
  --text-primary: #ffffff;
  --text-secondary: #9ca3af;
  --accent-purple: #a855f7;
  --accent-cyan: #22d3ee;
  --gradient-purple: linear-gradient(to bottom, #fff, #e9d5ff, #a855f7);
  
  /* 弹幕颜色 */
  --danmaku-red: #FF4500;
  --danmaku-green: #ADFF2F;
  --danmaku-cyan: #00FFFF;
  --danmaku-pink: #FF69B4;
  --danmaku-gold: #FFD700;
  --danmaku-white: #FFFFFF;
}
```

### 字体

- 主标题: `system-ui`, 衬线字体
- 数字: 等宽字体 `monospace`
- 正文: `system-ui`

---

## 🔧 模块设计

### 1. 星空背景 (starfield.js)

**原理**: 粒子从中心向四周发散，模拟太空飞行效果

```javascript
class Starfield {
  constructor(canvas) {
    this.stars = [];      // 星星数组
    this.speed = 2;       // 飞行速度
    this.starCount = 800; // 星星数量
  }
  
  // 核心方法
  init()        // 初始化星星
  update()      // 更新位置
  draw()        // 绘制
  animate()     // 动画循环
}
```

### 2. 烟花系统 (fireworks.js)

**原理**: 粒子发射 → 上升 → 爆炸 → 粒子扩散消失

```javascript
class Firework {
  constructor(x, y, targetY, color) {
    this.particles = [];   // 爆炸粒子
    this.exploded = false; // 是否已爆炸
  }
}

class FireworkSystem {
  constructor(canvas) {
    this.fireworks = [];
  }
  
  launch()      // 发射烟花
  update()      // 更新状态
  draw()        // 绘制
}
```

### 3. 倒计时 (countdown.js)

**原理**: 计算目标时间差，每秒更新DOM

```javascript
class Countdown {
  constructor(targetDate) {
    this.targetDate = new Date('2026-01-01T00:00:00');
  }
  
  calculate()   // 计算剩余时间
  updateDOM()   // 更新页面显示
  start()       // 启动倒计时
}
```

### 4. 弹幕系统 (danmaku.js)

**原理**: CSS动画驱动，随机生成位置/颜色/速度

```javascript
class Danmaku {
  constructor(container) {
    this.wishes = [...];  // 预设祝福语
    this.colors = [...];  // 颜色数组
  }
  
  createDanmaku(text)  // 创建弹幕元素
  addWish(text)        // 添加用户愿望
  startAutoPlay()      // 自动播放预设弹幕
}
```

---

## 🎬 动画设计

### 弹幕动画
```css
@keyframes danmaku {
  from { transform: translateX(100vw); }
  to { transform: translateX(-100%); }
}
```

### 淡入上浮动画
```css
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

### 数字翻转动画
```css
@keyframes flip {
  0% { transform: rotateX(0); }
  50% { transform: rotateX(-90deg); }
  100% { transform: rotateX(0); }
}
```

---

## 📝 预设祝福语

```javascript
const wishes = [
  "身体健康，万事如意",
  "Keep coding, keep creating.",
  "愿所有梦想都成真",
  "暴富!",
  "家人平安",
  "升职加薪",
  "去环游世界",
  "Happy New Year!",
  "不再脱发",
  "代码无Bug",
  "工资翻倍",
  "天天开心",
  "心想事成",
  "money money money",
  "存款突破百万",
  "找到理想工作",
  "暴富！暴富！还是TM的暴富！",
  "买彩票中次大的，不多，够付个首付就行。",
  "清空购物车不用犹豫，喜欢就买。",
  "比2025年更开心、更顺、更牛逼！",
  // ... 更多祝福语
];
```

---

## 🚀 开发计划

### Phase 1: 基础框架 (30min)
- [x] 创建项目结构
- [ ] HTML页面骨架
- [ ] CSS基础样式

### Phase 2: 核心特效 (60min)
- [ ] 星空粒子背景
- [ ] 烟花系统

### Phase 3: 交互功能 (40min)
- [ ] 倒计时组件
- [ ] 弹幕系统
- [ ] 愿望输入

### Phase 4: 完善优化 (20min)
- [ ] 音效系统
- [ ] 响应式适配
- [ ] 性能优化

---

## 📌 注意事项

1. **音频自动播放**: 现代浏览器禁止自动播放音频，需用户交互后才能播放
2. **性能优化**: Canvas动画使用 `requestAnimationFrame`
3. **响应式**: 适配移动端和桌面端
4. **目标日期**: 2026年1月1日 00:00:00 (本地时间)

---

## 📚 参考资源

- 原网站: https://2026.xcodeman.com/
- Canvas烟花教程: https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API
- CSS动画: https://developer.mozilla.org/en-US/docs/Web/CSS/animation

---

*文档版本: 1.0*  
*创建日期: 2024-12-31*
